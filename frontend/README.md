This is the frontend React app. Put these files under a CRA project or adapt to your setup.

Environment:
- Create a .env file with REACT_APP_API set to your backend, e.g. REACT_APP_API=http://localhost:5000

Run:
1. npm install
2. npm start
